import styled from "styled-components";
import {font} from "../styles/Common";

export const SectionTitle = styled.h2`
    ${font({family: "'line-height', sans-serif", weight: 600, Fmax: 24, Fmin: 24})}
    letter-spacing: 0.96px;
    
   
`